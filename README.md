
## 🛠️ Installation Steps

1. Clone the repository

```Bash
git clone https://github.com/modarAlkasem/unifi-test-node.git
```

2. Create '.env'file directly inside 'unifi-test-node' folder and define the same environment variables in'.env.example' into it then put your data

3. Open terminal go to 'unifi-test-node' folder and run 
```Bash
npm install
```

4. Run the project 

```Bash
npm start
```
